#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include "node.h"


/*Node newNode(int value){
	Node *newNode = (Node*)malloc(sizeof(Node));
	newNode->data = value;
	newNode->left == NULL;
	newNode->right == NULL;
    return *newNode;
}
*/

